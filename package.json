{
  "name": "habit-tracker",
  "version": "1.0.0",
  "description": "A minimal web app to track daily habits and build consistency.",
  "repository": "https://github.com/quantum2code/habit-tracker",
  "private": true,
  "workspaces": [
    "web",
    "server"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=web",
    "dev:server": "npm run dev --workspace=server"
  },
  "dependencies": {
    "mongodb": "^7.2.0"
  }
}
